import blockContent from './blockContent'
import post from './post'
import page from './page'
import siteSettings from './siteSettings'

export const schemaTypes = [post, page, blockContent, siteSettings]
